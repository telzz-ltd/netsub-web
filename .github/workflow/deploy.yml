name: Build, Test and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Use Node.JS
      - uses: oven-sh/setup-bun@v2
      - run: bun ci
      - run: bun run build
